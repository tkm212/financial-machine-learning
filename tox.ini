[tox]
skipsdist = true
envlist = py314

[gh-actions]
python =
    3.14: py314

[testenv]
passenv = PYTHON_VERSION
allowlist_externals = uv
commands =
    uv sync --python {envpython}
    uv run python -m pytest
    uv run ty check
